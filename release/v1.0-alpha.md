# libparser v1.0-alpha

> [!IMPORTANT}
> This version should not be used since the library is still in development

## parser API

```cpp
class parser final {
    public: // methods
        /**
          * @brief Get the parsed tokens in sequence
          */
        [[nodiscard]]
            auto tokens(void) const -> std::generator<token>;
};
```
